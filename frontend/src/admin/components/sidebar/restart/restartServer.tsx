import React from "react";
import './restartserver.scss';

export default function RestartServer() {
    return (
        <button className={"c-restart-server"}>
            Restart server
        </button>
    )
}