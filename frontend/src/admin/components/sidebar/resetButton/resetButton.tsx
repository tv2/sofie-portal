import React from "react";
import './resetButton.scss'

export default function ResetButton() {
    return (
        <button className={"c-restart-button"}>
            Restart server
        </button>
    )
}